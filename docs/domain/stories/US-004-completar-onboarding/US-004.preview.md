> ⚠️ **PROPUESTA PENDIENTE DE VALIDACIÓN CON DISEÑO** — Este prototipo debe ser revisado y aprobado por el equipo de diseño antes de implementarse.

```mermaid
flowchart TD
    %% Generado para HU #US-004: Marcar el onboarding como completado

    START(("Arranque de la app"))

    START --> READ_FLAG

    READ_FLAG("SonusNavHost lee\nAppSettings.onboardingCompleted\n(singleton id=1)")

    READ_FLAG --> GATE

    GATE{"¿onboardingCompleted\n== true?"}

    %% ---- Rama primera ejecución (flag = false) ----
    GATE -->|"false\n(primera ejecución / Big Bang §6.1)"| ONBOARDING

    subgraph FLUJO_ONBOARDING["Flujo de primera ejecución (EPIC-01)"]
        ONBOARDING["[US-001]\nConceder permisos"]
        SOURCE["[US-002]\nSelección de Carpetas Fuente"]
        SCAN["[US-003]\nEscaneo fundacional\n(con progreso)"]
        ONBOARDING -->|"Continuar"| SOURCE
        SOURCE -->|"Continuar"| SCAN
    end

    %% ---- Transición a biblioteca: disparo robusto por ambas ramas ----
    SCAN -->|"ScanState.Finished\n(escaneo normal)"| TRANSIT
    SCAN -->|"Biblioteca < 1s\n(transición fluida)"| TRANSIT

    TRANSIT(("Transición a la biblioteca"))

    TRANSIT --> COMPLETE

    subgraph CIERRE["Cierre del onboarding (silencioso — sin UI propia)"]
        COMPLETE("Emitir SettingsCommand.CompleteOnboarding\nTRG-CFG-02")
        COMPLETE --> IDEMPOTENT
        IDEMPOTENT{"¿Ya estaba\nen true?"}
        IDEMPOTENT -->|"Sí"| NOOP["No-op\n(idempotente, sin efecto)"]
        IDEMPOTENT -->|"No"| PERSIST
        PERSIST("CompleteOnboardingUseCase →\nSettingsRepository → SettingsDao\n(Room, local)")
        PERSIST --> PERSIST_RESULT
        PERSIST_RESULT{"¿Persistencia\nexitosa?"}
        PERSIST_RESULT -->|"OK"| DONE["onboardingCompleted = true\npersistido"]
        PERSIST_RESULT -->|"Fallo Room"| DEGRADE["OperationResult.Failure(DomainError)\nP1/P3: no aborta la transición;\nreintentable (idempotente)"]
    end

    NOOP --> LIBRARY
    DONE --> LIBRARY
    DEGRADE -.->|"La transición continúa;\nse reintenta en el próximo cierre"| LIBRARY

    %% ---- Rama arranque recurrente (flag = true) ----
    GATE -->|"true\n(arranque recurrente)"| LIBRARY

    subgraph VISTA_BIBLIOTECA["Vista Principal: Biblioteca"]
        LIBRARY["[Biblioteca Principal]\nCatálogo organizado, navegable\n(omite permisos/carpetas/escaneo)"]
    end

    %% ---- Nota de privacidad / autarquía ----
    subgraph INVARIANTES["Invariantes aplicadas"]
        NOTE["Solo estado operativo:\n- Sin dato comportamental [RNF-07]/Inv.3\n- Huella mínima, sin UI propia [RNF-08]\n- Persistencia local, sin red\n  Inv.1 / [RNF-06]"]
    end

    PERSIST -.->|"Restricciones"| NOTE

    style FLUJO_ONBOARDING fill:#e8f4fd,stroke:#1d3557
    style CIERRE fill:#f8f0ff,stroke:#6a4c93
    style VISTA_BIBLIOTECA fill:#2d6a4f,color:#fff,stroke:#1b4332
    style INVARIANTES fill:#fff3cd,stroke:#ffc107
    style DEGRADE fill:#fdecea,stroke:#c0392b
```
